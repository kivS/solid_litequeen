# Solid Lite Queen marketing pages

> Homepage, changelogs and guides section.

Static site workflow:

- `bun run dev` rebuilds on file changes and serves `dist/` locally.
- `bun run build` renders the site into `dist/`.
- `bun run serve` serves the generated static output with Bun.
